# yaml-language-server: $schema=ForagingController.json
startState: ScreenNorth_State1
states:
  ScreenNorth_State1:
    patchStates:
      Feeder1:
        behavior: Reward
        rewardRule:
          inactivityTimeout: 0.0
          distanceThreshold: 50.0
          nextState: ScreenNorth_State2
        led:
          active: false
      Feeder2:
        behavior: Reward
        rewardRule:
          inactivityTimeout: 0.0
          distanceThreshold: 50.0
          nextState: ScreenNorth_State2
        led:
          active: false
  ScreenNorth_State2:
    patchStates:
      Feeder1:
        behavior: Reward
        rewardRule:
          inactivityTimeout: 0.0
          distanceThreshold: 50.0
          nextState: ScreenNorth_State1
        led:
          active: false